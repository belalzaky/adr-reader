# adr-reader (ADR Reader — Project 7) — agent instructions

## Memory vault (read first, write back)
Belal's shared memory vault is the Obsidian vault at
`~/Claude/Projects/Income Generation/DATA + AI/`.
Before working here, read that vault's `00 Dashboard.md` and `Projects/ADR Reader (Project 7).md`.
When you change something, update `Projects/ADR Reader (Project 7).md` in the vault
(status, `updated` date, `next`, and a dated `## Log` bullet). Full rules: the vault's `CLAUDE.md`.
The vault is canonical.
